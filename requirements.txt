# Core dependencies
langchain-core>=0.1.0
langchain-ollama>=0.1.0
python-dotenv>=1.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Google API integration
google-api-python-client>=2.0.0
google-auth-httplib2>=0.1.0
google-auth-oauthlib>=1.0.0

# Web scraping
beautifulsoup4>=4.12.0
requests>=2.31.0

# Rich CLI for better terminal UI
rich>=13.0.0

# Web UI
streamlit>=1.28.0  # Legacy UI (can be removed later)
# FastAPI backend
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6  # For session middleware
starlette>=0.27.0  # For session support (included with FastAPI)
itsdangerous>=2.1.2  # Required by Starlette SessionMiddleware

# Testing
pytest>=7.0.0
pytest-mock>=3.10.0
playwright>=1.40.0

# Optional: For future auto-apply features
# selenium>=4.15.0
# playwright>=1.40.0
