# Skills

Prompts live here: `manifest.json` lists skills; each `skills/<id>/SKILL.md` holds instructions.

**Adding a skill:** See [docs/SKILLS.md](../docs/SKILLS.md). Or run `python scripts/scaffold_skill.py --id <id> --name "Name" --description "When to use."` and add the printed manifest entry.
